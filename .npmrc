# React Native / Expo + pnpm: linker plano (hoisted) para evitar problemas de
# resolución de módulos en Metro con un monorepo pnpm.
node-linker=hoisted
# Versiones exactas al instalar nuevas deps.
save-exact=false
